# =============================================================================
# PMS Environment Variables
# =============================================================================
# This file contains all hardcoded values from AWS Secrets Manager (pms/dev/*)
# These values match the Kubernetes deployment configuration
# 
# DO NOT commit sensitive values to production repositories!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (pms/dev/database)
# -----------------------------------------------------------------------------
POSTGRES_USER=pms
POSTGRES_PASSWORD=pms
POSTGRES_DB=pmsdb
DB_HOST=postgres
DB_PORT=5432
DB_NAME=pmsdb
DB_USERNAME=pms
DB_PASSWORD=pms

# -----------------------------------------------------------------------------
# Redis Configuration (pms/dev/redis)
# -----------------------------------------------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis

# -----------------------------------------------------------------------------
# RabbitMQ Configuration (pms/dev/rabbitmq)
# -----------------------------------------------------------------------------
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_DEFAULT_USER=rabbit-user
RABBITMQ_DEFAULT_PASS=rabbitmq
RABBITMQ_USERNAME=rabbit-user
RABBITMQ_PASSWORD=rabbitmq

# -----------------------------------------------------------------------------
# Kafka Configuration (pms/dev/kafka)
# -----------------------------------------------------------------------------
KAFKA_BOOTSTRAP_SERVERS=kafka:29092
KAFKA_ADMIN_PASSWORD=kafka
KAFKA_USER_PASSWORD=kafka

# -----------------------------------------------------------------------------
# Schema Registry Configuration (pms/dev/schema-registry)
# -----------------------------------------------------------------------------
SCHEMA_REGISTRY_URL=http://schema-registry:8081
SCHEMA_REGISTRY_API_KEY=registry-key-here
SCHEMA_REGISTRY_API_SECRET=registry-secret-here

# -----------------------------------------------------------------------------
# Auth Service Configuration (pms/dev/auth)
# -----------------------------------------------------------------------------
AUTH_JWT_SECRET=auth-jwt-secret-789
JWT_SECRET=auth-jwt-secret-789

# -----------------------------------------------------------------------------
# Simulation Service Configuration (pms/dev/simulation)
# -----------------------------------------------------------------------------
SIMULATION_API_KEY=sim-api-key-123
SIMULATION_JWT_SECRET=sim-jwt-secret-456

# -----------------------------------------------------------------------------
# Trade Capture Service Configuration (pms/dev/trade-capture)
# -----------------------------------------------------------------------------
TRADE_CAPTURE_API_KEY=tc-api-key-123
TRADE_CAPTURE_JWT_SECRET=tc-jwt-secret-456

# -----------------------------------------------------------------------------
# Validation Service Configuration (pms/dev/validation)
# -----------------------------------------------------------------------------
VALIDATION_API_KEY=val-api-key-123
VALIDATION_JWT_SECRET=val-jwt-secret-456

# -----------------------------------------------------------------------------
# Analytics Service Configuration
# -----------------------------------------------------------------------------
API_KEY=d4jsud1r01qgcb0vck8gd4jsud1r01qgcb0vck90

# -----------------------------------------------------------------------------
# Spring Profiles
# -----------------------------------------------------------------------------
SPRING_PROFILES_ACTIVE=docker

# -----------------------------------------------------------------------------
# Java Options (can be customized per service)
# -----------------------------------------------------------------------------
JAVA_OPTS_SMALL=-Xms256m -Xmx512m
JAVA_OPTS_MEDIUM=-Xms512m -Xmx1024m
JAVA_OPTS_LARGE=-Xms1024m -Xmx2048m
