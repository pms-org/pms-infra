# pms-platform/values.yaml

# Global toggles
postgres:
  enabled: true
rabbitmq:
  enabled: true
redis:
  enabled: true
kafka:
  enabled: true
schemaRegistry:
  enabled: true
externalSecrets:
  enabled: false

apigateway:
  enabled: true
auth:
  enabled: true
simulation:
  enabled: true
  deployment:
    healthChecks:
      enabled: false
tradeCapture:
  enabled: true
  deployment:
    healthChecks:
      enabled: false
validation:
  enabled: true
  deployment:
    healthChecks:
      enabled: false

# Optional shared values
global:
  environment: prod
