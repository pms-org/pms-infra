apiVersion: v1
kind: ConfigMap
metadata:
  name: pms-global-config
  namespace: {{ .Values.global.namespace }}
data:
  {{- range $key, $value := .Values.global.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
