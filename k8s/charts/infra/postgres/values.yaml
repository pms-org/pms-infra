# Default values for postgres chart
# This chart deploys PostgreSQL database

# Service configuration
service:
  name: postgres
  namespace: pms
  port: 5432
  targetPort: 5432
  type: ClusterIP

# Deployment configuration
deployment:
  replicas: 1
  image:
    repository: postgres
    tag: "16"
    pullPolicy: IfNotPresent

  # Resource limits and requests
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 1Gi

# Persistence configuration
persistence:
  enabled: true
  accessModes:
    - ReadWriteOnce
  size: 1Gi
  storageClassName: "" # Use default storage class

# Database configuration (non-secret)
config:
  POSTGRES_DB: pmsdb
  POSTGRES_USER: pms

# Secrets configuration (AWS Secrets Manager)
secrets:
  # AWS Secrets Manager secret path
  path: pms/dev/database

  # Refresh interval
  refreshInterval: 1h

  # Secret key mappings - map canonical AWS names to postgres-specific names
  data:
    - secretKey: POSTGRES_PASSWORD
      remoteKey: DB_PASSWORD

# Seed data configuration
# This controls the Helm hook job that populates reference data
seedData:
  enabled: true

  # Portfolio investor details - reference data for validation
  portfolios:
    - id: "3f2c1d4e-8b57-4a92-9c65-b5e6f4e19b73"
      investorName: "Portfolio A"
      phoneNumber: 1234567890
      address: "New York"

    - id: "7a9e3f1c-2b8d-4e6a-a1c5-d7f8e9a4b6c2"
      investorName: "Portfolio B"
      phoneNumber: 9876543210
      address: "London"

    - id: "5b1d8f3e-9c4a-4d7e-b2f6-a8c9e1d4f7b3"
      investorName: "Portfolio C"
      phoneNumber: 5551234567
      address: "Tokyo"

    - id: "9e4c2a1f-7d6b-4e8c-a3f5-b9d1e7c4a6f2"
      investorName: "Portfolio D"
      phoneNumber: 4445556666
      address: "Singapore"

    - id: "1f7e9b3d-5a4c-4e2f-b8d6-c1a9e4f7b3d5"
      investorName: "Portfolio E"
      phoneNumber: 3334445555
      address: "Frankfurt"

  # Stock symbols - reference data for trade validation
  stocks:
    - symbol: "AAPL"
      sector: "TECH"

    - symbol: "GOOGL"
      sector: "TECH"

    - symbol: "MSFT"
      sector: "TECH"

    - symbol: "AMZN"
      sector: "TECH"

    - symbol: "TSLA"
      sector: "AUTO"

    - symbol: "JPM"
      sector: "FINANCE"

    - symbol: "BAC"
      sector: "FINANCE"

    - symbol: "GS"
      sector: "FINANCE"

    - symbol: "V"
      sector: "FINANCE"

    - symbol: "JNJ"
      sector: "HEALTHCARE"

    - symbol: "PFE"
      sector: "HEALTHCARE"

    - symbol: "UNH"
      sector: "HEALTHCARE"

    - symbol: "XOM"
      sector: "ENERGY"

    - symbol: "CVX"
      sector: "ENERGY"

    - symbol: "WMT"
      sector: "RETAIL"
