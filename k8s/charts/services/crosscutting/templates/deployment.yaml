apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.service.name }}
  namespace: {{ .Values.service.namespace }}
spec:
  replicas: {{ .Values.deployment.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.service.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.service.name }}
    spec:
      initContainers:
      {{- range .Values.deployment.dependencies }}
      {{- if .enabled }}
      - name: wait-for-{{ .name }}
        image: {{ .image }}
        command:
          {{- toYaml .command | nindent 10 }}
      {{- end }}
      {{- end }}
      containers:
      - name: {{ .Values.service.name }}
        image: {{ .Values.deployment.image.repository }}:{{ .Values.deployment.image.tag }}
        imagePullPolicy: {{ .Values.deployment.image.pullPolicy }}
        resources: {{ toYaml .Values.deployment.resources | nindent 10 }}
        envFrom:
        # Global config (REDIS_HOST, REDIS_PORT, etc.)
        - configMapRef:
            name: pms-global-config
        # Service-specific config
        - configMapRef:
            name: {{ .Values.service.name }}-config
        # Global secrets (REDIS_PASSWORD)
        - secretRef:
            name: pms-global-secrets
        {{- if .Values.secrets.data }}
        # Service-specific secrets (if any)
        - secretRef:
            name: {{ .Values.service.name }}-secrets
        {{- end }}
        ports:
        - containerPort: {{ .Values.service.targetPort }}
        {{- if .Values.deployment.healthChecks.enabled }}
        {{- include "crosscutting.livenessProbe" . | nindent 8 }}
        {{- include "crosscutting.readinessProbe" . | nindent 8 }}
        {{- include "crosscutting.startupProbe" . | nindent 8 }}
        {{- end }}
