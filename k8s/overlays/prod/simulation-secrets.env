# Production Secrets for Simulation Service
# IMPORTANT: Update these values with actual production credentials
# Consider using External Secrets Operator or Sealed Secrets in real production
SPRING_DATASOURCE_USERNAME=pms_prod
SPRING_DATASOURCE_PASSWORD=CHANGEME_PROD_PASSWORD
