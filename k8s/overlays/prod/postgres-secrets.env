# Production Secrets for Postgres
# IMPORTANT: Update these values with actual production credentials
# In production, use AWS RDS instead of in-cluster Postgres
POSTGRES_USER=pms_prod
POSTGRES_PASSWORD=CHANGEME_PROD_PASSWORD
POSTGRES_DB=pmsdb_prod
